<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chatapp.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.chatapp.entity.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="email" column="email" />
        <result property="nickname" column="nickname" />
        <result property="avatar" column="avatar" />
        <result property="avatarData" column="avatar_data" />
        <result property="avatarContentType" column="avatar_content_type" />
        <result property="status" column="status" />
        <result property="signature" column="signature" />
        <result property="phone" column="phone" />
        <result property="gender" column="gender" />
        <result property="birthday" column="birthday" />
        <result property="lastLoginTime" column="last_login_time" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="deleted" column="deleted" />
    </resultMap>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username} AND deleted = 0
    </select>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email} AND deleted = 0
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insert" parameterType="com.chatapp.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email, nickname, avatar, avatar_data, avatar_content_type, status, signature, phone, gender, birthday, last_login_time, create_time, update_time)
        VALUES (#{username}, #{password}, #{email}, #{nickname}, #{avatar}, #{avatarData}, #{avatarContentType}, #{status}, #{signature}, #{phone}, #{gender}, #{birthday}, #{lastLoginTime}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.chatapp.entity.User">
        UPDATE users 
        <set>
            <if test="email != null">email = #{email},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="avatarData != null">avatar_data = #{avatarData},</if>
            <if test="avatarContentType != null">avatar_content_type = #{avatarContentType},</if>
            <if test="status != null">status = #{status},</if>
            <if test="signature != null">signature = #{signature},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthday != null">birthday = #{birthday},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updatePassword">
        UPDATE users SET password = #{newPassword}, update_time = NOW() 
        WHERE id = #{userId} AND deleted = 0
    </update>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) FROM users WHERE username = #{username} AND deleted = 0
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) FROM users WHERE email = #{email} AND deleted = 0
    </select>

    <update id="updateLastLoginTime">
        UPDATE users SET last_login_time = NOW(), update_time = NOW() 
        WHERE id = #{userId} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE users SET deleted = 1, update_time = NOW() WHERE id = #{id}
    </update>

    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM users WHERE deleted = 0
    </select>

</mapper>